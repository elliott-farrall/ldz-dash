from pathlib import Path

SRC = Path(__file__).parent

BLUEPRINTS_DIR = SRC / "blueprints"
STATIC_DIR = SRC / "static"
TEMPLATES_DIR = SRC / "templates"

OPTIONS_DIR = SRC / "options"

DATA_DIR = SRC.parent / "data"

USERS_FILE = SRC.parent / "USERS"
